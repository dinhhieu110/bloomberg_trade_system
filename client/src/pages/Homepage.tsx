import React from 'react';
import { Hero } from '../components';

type Props = {};

const Homepage = (props: Props) => {
  return (
    <div>
      <Hero />
    </div>
  );
};

export default Homepage;
